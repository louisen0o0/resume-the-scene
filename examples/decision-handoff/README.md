# Decision handoff fixture

This fixture represents a worker change after one piece of evidence has already been verified and one decision is still active.

The next worker must load both `#e4` and `#d4`, skip the verified evidence, and continue at `#a4` without reopening the active decision.
