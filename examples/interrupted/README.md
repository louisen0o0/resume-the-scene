# Interrupted task fixture

This fixture represents a task that stopped after verified work existed. The resume packet keeps the verified work in `skip` and points to one exact next action instead of replaying the history.
