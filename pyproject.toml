[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "resume-the-scene"
version = "0.1.0"
description = "task-state-project-memory"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
resume-scene = "resume_scene.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
